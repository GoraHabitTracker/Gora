<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/sea_foam">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/container_content"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="70dp"
        app:defaultNavHost="true"
        app:navGraph="@navigation/navigation_bottom"
        tools:layout="@layout/fragment_home" />

    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottom_app_bar"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_gravity="bottom"
        app:elevation="0dp"
        app:fabCradleVerticalOffset="10dp">
    </com.google.android.material.bottomappbar.BottomAppBar>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:paddingHorizontal="34dp"
        android:layout_gravity="bottom"
        android:paddingTop="14dp"
        app:elevation="0dp"
        app:itemIconTint="@color/blue_fcbk"
        app:menu="@menu/menu_bottom" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:foregroundGravity="center"
        android:contentDescription="@string/go"
        app:elevation="0dp"
        app:layout_anchor="@id/bottom_app_bar"/>
</androidx.coordinatorlayout.widget.CoordinatorLayout>


    <!--    android:stateListAnimator="@null"       - убирает тени с кнопока -->
    <!--    app:elevation="0dp"                     - убирает тени в нижней навигации-->
    <!--    app:itemIconTint="@color/blue_fcbk"     - устанавливает цвет иконкам -->